
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*

# google cloud sdk
export PATH=$PATH:~/google-cloud-sdk/bin

# The next line updates PATH for the Google Cloud SDK.
source /Users/thagikura/google-cloud-sdk/path.zsh.inc

# The next line enables bash completion for gcloud.
source /Users/thagikura/google-cloud-sdk/completion.zsh.inc


# go
GOROOT=/usr/local/go
GOPATH=$GOROOT/bin

# Android AOSP
export PATH=$PATH:~/bin

export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.7.0_60.jdk/Contents/Home

source $(brew --prefix nvm)/nvm.sh
nvm use 0.11

if [ -d /Users/thagikura/google3_src/authenticator/google3/util/shell/gbash ];then 
  export PATH=$PATH:/Users/thagikura/google3_src/authenticator/google3/util/shell/gbash
fi

export ANDROID_HOME=~/android_sdk
